Just a simple seed for a "Hello World" angular-js boostrap application

First time
# bower init
# bower install angular-bootstrap --save //Boostrap JS stuff for Angular JS without CSS
# bower install bootstrap --save		 //The Boostrapp stuff (because we need the CSS)

This will create a bower.json 

---

After a fresh clone just run :
# bower install

